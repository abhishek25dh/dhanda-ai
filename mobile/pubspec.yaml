name: dhanda_ai
description: Android app for recording rewritten channel scripts.
publish_to: "none"
version: 0.1.3+4

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/dhanda_logo.png
